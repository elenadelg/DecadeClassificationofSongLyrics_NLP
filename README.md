# -DecadeClassificationofSongLyrics_NLP
 The project utilizes NLP to predict release decade of songs from their lyrics. It compares the performance across different models i.e. Logistic Regression, Random Forest, LSTM, and BERT, plus a large language model (LlaMA).
